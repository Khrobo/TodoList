class TodoProjects {
    constructor(title, task, date, check) {
        this.title = title;
        this.task = task;
        this.date = date;
        this.check = check;
    }
}

const projectArray = [];

export { TodoProjects, projectArray }
